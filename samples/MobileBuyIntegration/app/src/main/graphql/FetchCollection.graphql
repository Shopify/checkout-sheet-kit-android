query FetchCollection($handle: String!, $numProducts: Int!) {
  collection(handle: $handle) {
    id
    handle
    title
    description
    image {
      url
      altText
      width
      height
    }
    products(first: $numProducts) {
      edges {
        node {
          ...ProductFragment
        }
      }
    }
  }
}
